services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4200:80"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
